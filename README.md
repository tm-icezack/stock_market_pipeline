# Stock Market Pipeline

## Local services

Start the Airflow stack from the `airflow` directory with Docker Compose. The Airflow UI is available at [http://localhost:8081](http://localhost:8081), and PostgreSQL is exposed locally on port `5433`.
